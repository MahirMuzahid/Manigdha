<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Manigdha.View.BuyPost"
             Title="Buy Post">
    <Grid BackgroundColor="{AppThemeBinding Light={StaticResource LightWhite}, Dark={StaticResource Gray950}}">
        <Grid  RowDefinitions="50,*" Margin="0">
            <Grid Grid.Row="0"  RowDefinitions="*,45,*" ColumnDefinitions="*,50,20">
                <Frame Grid.ColumnSpan="3" Grid.RowSpan="2"  BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}" BorderColor="Transparent">

                </Frame>
                
                <SearchBar Grid.Column="0"  Grid.Row="1" HeightRequest="20" Placeholder="Search"/>
                <Button Text="Filter" Grid.Row="1" Grid.Column="1" FontSize="10" FontAttributes="Bold" Margin="0,10,0,10" />
            </Grid>
            
            <ScrollView Grid.Row="1" Margin="0,0,0,0">
                <ListView x:Name="mockList" SeparatorColor="Transparent" HasUnevenRows="True" BackgroundColor="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid Margin="0,10,0,0">
                                    
                                    <Frame  BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}" CornerRadius="10" BorderColor="Transparent">
                                        
                                    </Frame>
                                    
                                    <Grid RowDefinitions="AUTO,250,AUTO">
                                        <Grid Grid.Row="0" RowDefinitions="AUTO,AUTO,AUTO" Padding="10">
                                            <Label Text="Mahir Muzahid" HorizontalOptions="Start" VerticalOptions="Center" Grid.Row="0" FontSize="12" FontAttributes="Bold"/>
                                            <Label Text="2 Days Ago" HorizontalOptions="Start" VerticalOptions="Center" Grid.Row="1" FontSize="10"/>
                                            <Label Text="This is a title. Where seller will post about product" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" Grid.Row="2" FontSize="12" Margin="0,5,0,0"/>
                                        </Grid>
                                        
                                        <Image Source="product.jpg" Grid.Row="1" Aspect="AspectFill"/>
                                       

                                        <Grid Grid.Row="2" RowDefinitions="AUTO"  ColumnDefinitions="*,*" Padding="10">
                                            <Button  Text="Detail" Grid.Column="0" BackgroundColor="Transparent" FontAttributes="Bold" BorderWidth="0" TextColor="{AppThemeBinding Light= Black, Dark= White}" ></Button>
                                            <Label Grid.Row="0" Grid.Column="1" HorizontalOptions="Center" VerticalOptions="Center"  >
                                                <Label.FormattedText>
                                                    <FormattedString>

                                                        <Span Text="700" FontAttributes="Bold" TextColor="{AppThemeBinding Light= Black, Dark= White}"/>
                                                        <Span Text=" Taka" FontAttributes="Bold" TextColor="{AppThemeBinding Light= Black, Dark= White}"/>
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                        </Grid>
                                       
                                    </Grid>
                                   
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollView>
        </Grid>
        
    </Grid>
</ContentPage>